{
  "name": "lifelog",
  "version": "1.0.0",
  "private": true,
  "description": "Lifelog — Phase 1: Conversation Understanding & Structured Life Data Engine",
  "type": "module",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run test --workspace backend",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:generate": "npm run db:generate --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "keys:check": "node scripts/check-secrets.mjs",
    "keys:init": "node scripts/init-keys.mjs",
    "hooks:install": "node scripts/install-hooks.mjs"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "overridesRationale": {
    "esbuild": "drizzle-kit still pulls an old esbuild through @esbuild-kit/*. That esbuild lets any website read its dev server's responses (GHSA-67mh-4wv8-2f99). It is a build-time-only tool, but the fix is a one-line pin, so pin it. Re-check with `npm audit` after upgrading drizzle-kit."
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
